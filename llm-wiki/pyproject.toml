[project]
name = "llm-wiki-example"
version = "0.1.0"
description = "LLM wiki using Deep Agents (local offline execution)"
requires-python = ">=3.11"
dependencies = [
    "deepagents>=0.6.8",
    "pytest>=8.0.0",
]



[tool.uv.sources]
deepagents = { path = "../../deepagents/libs/deepagents", editable = true }

